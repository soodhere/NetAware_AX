# Cadence 7 — configured operator readiness (not live discovery).
# ECS is operator infrastructure. No CAMARA ECS API. No live Telco Finder access-type.

source: CONFIGURED_OPERATOR_READINESS

model:
  cadence: 7
  executable: false
  source: CONFIGURED_OPERATOR_READINESS
  accessTypeSource: RUNTIME_CLIENT_CONTEXT

operators:
  - providerId: simulated-operator-a
    kind: operator
    nv1Supported: true
    nv2Supported: true
    entitlementServer:
      available: AVAILABLE
      supportsOperatorToken: true
    note: Configured demo readiness. Not a live ECS probe.

  - providerId: simulated-operator-b
    kind: operator
    nv1Supported: true
    nv2Supported: true
    entitlementServer:
      available: UNAVAILABLE
      supportsOperatorToken: false
    note: Number Verification API is advertised. Entitlement Server is unavailable. Cadence 9 Wi-Fi ECS-gap seed. Configured operator readiness, not a live ECS probe.

  - providerId: simulated-aggregator-b
    kind: aggregator
    nv1Supported: true
    nv2Supported: true
    entitlementServer:
      available: UNKNOWN
      supportsOperatorToken: false
    note: Aggregator is not the ECS owner. Serving-operator ECS still required for NV2.

  - providerId: simulated-provider-c
    kind: specialist
    nv1Supported: false
    nv2Supported: false
    entitlementServer:
      available: UNAVAILABLE
      supportsOperatorToken: false

runtimePrerequisites:
  fields:
    - ts43.clientAvailable
    - simAvailable
    - operatorTokenAvailable
    - tokenPathState
  tokenPathStateValues: [UNAVAILABLE, NETWORK_BASED, OPERATOR_TOKEN, UNKNOWN]
  note: Device/runtime prerequisites come from client context, not Telco Finder.

futureEcsUnavailableSeed:
  id: operator-a-ecs-down
  providerId: simulated-operator-a
  entitlementServer:
    available: UNAVAILABLE
    supportsOperatorToken: false
  usedBy: Cadence 9 alternate overlay. Live Wi-Fi ECS-gap variant uses Network Provider B (simulated-operator-b) so API Finder still shows Number Verification available.
  executableNow: true
