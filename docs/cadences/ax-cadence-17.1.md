# Cadence 17.1 — Visual storytelling polish

Status: complete. Do not start Cadence 18. Do not deploy unless separately instructed.

Cadence 17.1 does not change the product model, runtime, portfolio, catalog, Meeting Mode paths, Basic Auth, or deployment architecture. It makes the existing Cadence 17 visuals obvious for sales.

Live version remains `0.6.1-ax6.1`. Model cadence remains 7. UI cadence remains **17** with patch **17.1**.

## Inspected first

KEEP: 17×13 matrix, NV path tree, High Flight baggage world, Inspection loop, OTA funnel, CityCare min, evidence reuse, Finder stages, heatmap, Demand Map, Meeting Mode paths.

IMPROVE: AX Brain (from AxLoop / agentic chain), discovery funnel (catalog universe vs generated candidates), reverse map as graph, matrix focus+legend, configured+runtime, governance gates, topology region lanes, DX→AX as without/with, close flywheel, High Flight lane labels, inspection HTTP vs objective, OTA reuse overlay.

MERGE: AxLoop + AgenticLoopVisual into AxBrain as the memorable mental model. Keep AxLoop for compact plan steps.

DO NOT BUILD: second mapping database, graph library, duplicate NV engine, revenue visuals, a separate visual demo.

## Five signature visuals

1. INTENT → AX → OUTCOME (`AxBrain`)
2. Capability narrowing (`DiscoveryFunnel`)
3. Use Case ↔ API graph + kept 17×13 matrix
4. Configured knowledge + runtime discovery
5. API exposed → ready → fulfillable → demand served (`OperatorLadder`)

Presenter cues stay hidden unless Show presenter cues is on.

## Honesty

Derived from existing `/map`, `/coverage`, `/meet`, and live traces only. Catalog universe is not implied to be the runtime candidate set.
