openapi: 3.0.3
info:
  title: Network Health Assessment
  description: |
    Network Insight offers a unified analytical platform for specific networks, such as private networks and slices, focusing exclusively on aggregated network-level data rather than individual device information.
    It empowers users to both evaluate the overall network health through a comprehensive, component-based scoring system and to analyze service traffic patterns with customizable granularity.
    By providing a holistic view — from infrastructure health to user traffic trends — this API suite delivers the critical, real-time data needed for comprehensive network monitoring, strategic performance optimization, and effective early warning systems.

    # Introduction
    This API allows users to query the health score of a specified network, which consists of multiple components including the wireless network, transmission network, and core network.
    By calculating the health score for each component and then applying weighted calculations, the API provides a comprehensive health score result for the specified network.

    The health score is a value evaluated by the platform based on the performance/fault indicator status of a specific network module, ranging from 0 to 100. Scores above 90 are considered excellent/healthy,
    scores between 60 and 80 indicate varying degrees of issues, and scores below 60 mean the network is basically unable to provide normal services.
    A specified network contains different network modules, such as wireless networks, transport networks, and core networks.
    The "netType" parameter can be used to query the health assessment of the corresponding network module or the entire network.
    The health assessment mechanism periodically evaluates the performance and alarm data of network devices and generates reports.
    This API provides the latest report results by default.

    # API functionality
    The API provides the following functionality:
    - Query the health scores of different network modules in the specified network at the latest point in time.

    # Request Parameters Definition:
    * **networkId**: Unique identifier of the Assessment network (UUID format).
    * **netType**: Modules within the network. Optional values are: NET (the entire network), NET_WIRELESS (wireless network), NET_TRANSPORT (transport network), NET_CORE (core network).

    # Privacy and Data Granularity

    This API provides health assessment data at the network and network-module level.
    All data exposed by this API is aggregated and does not contain individual device, user, or terminal identifiers.

    **Authentication:** This API uses two-legged access tokens (Client Credentials grant type)
    for service-to-service authentication. No end-user personal data is processed by this API,
    therefore no user consent or three-legged tokens are required.

    <!-- CAMARA:MANDATORY:authorization-and-authentication:BEGIN -->
    # Authorization and authentication

    The "Camara Security and Interoperability Profile" provides details of how an API consumer requests an access token. Please refer to Identity and Consent Management (https://github.com/camaraproject/IdentityAndConsentManagement/) for the released version of the profile.

    The specific authorization flows to be used will be agreed upon during the onboarding process, happening between the API consumer and the API provider, taking into account the declared purpose for accessing the API, whilst also being subject to the prevailing legal framework dictated by local legislation.

    In cases where personal data is processed by the API and users can exercise their rights through mechanisms such as opt-in and/or opt-out, the use of three-legged access tokens is mandatory. This ensures that the API remains in compliance with privacy regulations, upholding the principles of transparency and user-centric privacy-by-design.
    <!-- CAMARA:MANDATORY:authorization-and-authentication:END -->

    <!-- CAMARA:MANDATORY:request-body-strictness:BEGIN -->
    # Request body strictness

    This API rejects requests with JSON request bodies that contain properties not declared in this specification, at any nesting level. Unknown properties result in a `400 INVALID_ARGUMENT` response.
    <!-- CAMARA:MANDATORY:request-body-strictness:END -->

    <!-- CAMARA:MANDATORY:additional-error-responses:BEGIN -->
    # Additional CAMARA error responses

    The list of error codes in this API specification is not exhaustive. Therefore the API specification MAY not document some non-mandatory error statuses as indicated in `CAMARA API Design Guide`.

    Please refer to the `CAMARA_common.yaml` of the Commonalities Release associated to this API version for a complete list of error responses. The applicable Commonalities Release can be identified in the `API Readiness Checklist` document associated to this API version.

    As a specific rule, error `501 - NOT_IMPLEMENTED` can be only a possible error response if it is explicitly documented in the API.
    <!-- CAMARA:MANDATORY:additional-error-responses:END -->
  version: 0.1.0
  x-camara-commonalities: 0.8.0
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
servers:
  - url: '{apiRoot}/network-health-assessment/v0.1'
    variables:
      apiRoot:
        default: http://localhost:9091
        description: API root, defined by the service provider, e.g. `api.example.com` or `api.example.com/somepath`
tags:
  - name: Network Health Assessment
    description: Query Network Health Assessment scores for specific network modules.
externalDocs:
  description: Project documentation at CAMARA
  url: https://github.com/camaraproject/NetworkInsights
paths:
  /health-scores:
    get:
      tags:
        - Network Health Assessment
      summary: Query the latest network health score
      description: |
        Query the health assessment results of the specified network module at the latest time point.
        Returns the health score (0-100) for the selected network module.
        If the specified `networkId` exists but no health data is available, returns `200 OK` with `score: null` and `scoringTime: null`.
      operationId: getHealthScores
      security:
        - openId:
            - network-health-assessment:health-scores:read
      parameters:
        - $ref: '#/components/parameters/x-correlator'
        - name: networkId
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/NetworkId'
          description: Unique identifier of the Assessment network (UUID format).
          example: 123e4567-e89b-12d3-a456-426614174000
        - name: netType
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/NetTypeEnum'
          description: Modules within the network.
          example: NET
      responses:
        '200':
          description: 'Health score queried successfully. Returns `score: null` and `scoringTime: null` if no data is available for the specified network/module.'
          headers:
            x-correlator:
              $ref: '#/components/headers/x-correlator'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthInfo'
              examples:
                RESPONSE_NET:
                  $ref: '#/components/examples/RESPONSE_EXAMPLE_NET'
                RESPONSE_NET_WIRELESS:
                  $ref: '#/components/examples/RESPONSE_EXAMPLE_NET_WIRELESS'
                RESPONSE_NO_DATA:
                  $ref: '#/components/examples/RESPONSE_EXAMPLE_NO_DATA'
        '400':
          $ref: '#/components/responses/Generic400'
        '401':
          $ref: '#/components/responses/Generic401'
        '403':
          $ref: '#/components/responses/Generic403'
        '404':
          $ref: '#/components/responses/Generic404'
components:
  securitySchemes:
    openId:
      description: OpenID Connect authentication
      type: openIdConnect
      openIdConnectUrl: https://example.com/.well-known/openid-configuration
  parameters:
    x-correlator:
      name: x-correlator
      in: header
      description: Correlation id for the different services
      schema:
        $ref: '#/components/schemas/XCorrelator'
  headers:
    x-correlator:
      description: Correlation id for the different services
      schema:
        $ref: '#/components/schemas/XCorrelator'
  schemas:
    NetworkId:
      description: Network ID in UUID format (unique identifier of the analyzed network)
      type: string
      format: uuid
      maxLength: 36
      example: 123e4567-e89b-12d3-a456-426614174000
    Score:
      title: HealthScore
      description: Value of the health score (0-100, higher is better). Returns `null` if no health data is available for the specified network/module.
      type: number
      format: double
      minimum: 0
      maximum: 100
      nullable: true
      example: 94.56
    HealthInfo:
      title: NetworkHealthInfo
      description: Network health score information. When no data is available, `score` and `scoringTime` are `null`.
      allOf:
        - type: object
          properties:
            networkId:
              $ref: '#/components/schemas/NetworkId'
            netType:
              $ref: '#/components/schemas/NetTypeEnum'
            score:
              $ref: '#/components/schemas/Score'
            scoringTime:
              type: string
              format: date-time
              nullable: true
              maxLength: 64
              description: Latest time point of the health score calculation. Returns `null` if no health data is available. It must follow [RFC 3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) and must have time zone.
              example: '2025-12-01T12:00:00Z'
          required:
            - networkId
            - netType
            - score
            - scoringTime
    NetTypeEnum:
      title: NetworkModuleType
      description: Enums of network module types
      type: string
      enum:
        - NET
        - NET_WIRELESS
        - NET_TRANSPORT
        - NET_CORE
      example: NET
    XCorrelator:
      type: string
      description: Correlator string, UUID format recommended but any string matching the pattern can be used
      pattern: ^[a-zA-Z0-9-_:;.\/<>{}]{0,256}$
      maxLength: 256
      example: b4333c46-49c0-4f62-80d7-f0ef930f1c46
    ErrorInfo:
      type: object
      description: A structured error response providing details about a failed request, including the HTTP status code, an error code, and a human-readable message
      required:
        - status
        - code
        - message
      properties:
        status:
          type: integer
          format: int32
          minimum: 100
          maximum: 599
          description: HTTP response status code
        code:
          type: string
          maxLength: 96
          description: A human-readable code to describe the error
        message:
          type: string
          maxLength: 512
          description: A human-readable description of what the event represents
  responses:
    Generic400:
      description: Bad Request
      headers:
        x-correlator:
          $ref: '#/components/headers/x-correlator'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorInfo'
          examples:
            GENERIC_400_INVALID_ARGUMENT:
              description: Invalid Argument. Generic Syntax Exception
              value:
                status: 400
                code: INVALID_ARGUMENT
                message: Client specified an invalid argument, request body or query param.
            GENERIC_400_OUT_OF_RANGE:
              description: Out of Range. Specific Syntax Exception used when a given field has a pre-defined range or a invalid filter criteria combination is requested
              value:
                status: 400
                code: OUT_OF_RANGE
                message: Client specified an invalid range.
    Generic401:
      description: Unauthorized
      headers:
        x-correlator:
          $ref: '#/components/headers/x-correlator'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorInfo'
          examples:
            GENERIC_401_UNAUTHENTICATED:
              description: Request cannot be authenticated and a new authentication is required
              value:
                status: 401
                code: UNAUTHENTICATED
                message: Request not authenticated due to missing, invalid, or expired credentials. A new authentication is required.
    Generic403:
      description: Forbidden
      headers:
        x-correlator:
          $ref: '#/components/headers/x-correlator'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorInfo'
          examples:
            GENERIC_403_PERMISSION_DENIED:
              description: Permission denied. OAuth2 token access does not have the required scope or when the user fails operational security
              value:
                status: 403
                code: PERMISSION_DENIED
                message: Client does not have sufficient permissions to perform this action.
    Generic404:
      description: Not found
      headers:
        x-correlator:
          $ref: '#/components/headers/x-correlator'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorInfo'
          examples:
            GENERIC_404_NOT_FOUND:
              description: Resource is not found
              value:
                status: 404
                code: NOT_FOUND
                message: The specified resource is not found.
  examples:
    RESPONSE_EXAMPLE_NET:
      description: Example response for health score of the entire network (NET type)
      value:
        networkId: 123e4567-e89b-12d3-a456-426614174000
        netType: NET
        score: 94.56
        scoringTime: '2025-12-01T12:00:00Z'
    RESPONSE_EXAMPLE_NET_WIRELESS:
      description: Example response for health score of the wireless network module (NET_WIRELESS type)
      value:
        networkId: 123e4567-e89b-12d3-a456-426614174000
        netType: NET_WIRELESS
        score: 90.12
        scoringTime: '2025-12-01T12:00:00Z'
    RESPONSE_EXAMPLE_NO_DATA:
      description: Example response when no health data is available for the specified network/module
      value:
        networkId: 123e4567-e89b-12d3-a456-426614174000
        netType: NET
        score: null
        scoringTime: null
