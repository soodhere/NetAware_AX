# Frontend — AX Cadence 2

Home + Explore + Rocket Bank live run. Other intents are not executed.

```powershell
cd backend
python -m uvicorn app.main:app --port 8000 --host 127.0.0.1
```

```powershell
cd frontend
npm install
npm run dev
```

Open http://127.0.0.1:5173

Start Demo → Rocket Bank → High-value transaction protection → **Run Intent**.
